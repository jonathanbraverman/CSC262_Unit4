var mynumber;
function addup(x, y) {
    var z = x + y;
    return z;
}
mynumber = addup(1, 2);
console.log(mynumber);
// mynumber = function (x : number, y : number) : number
var myAdd = function (x, y) {
    return x + y;
};
